### A little collection of qtct themes

All screenshots are aviable in subfolders. More themes are planned to be added in the future.

### Installation 

Put needed files to `$HOME/.config/qt5ct/colors/` or `$HOME/.config/qt6ct/colors/`
